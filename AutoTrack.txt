-- +======================================================+
-- |            AUTO TRACK v5  |  WindUI Edition          |
-- |  Enemy ESP + Auto Track + Hitbox + Auto Bomb         |
-- +======================================================+
local Players       = game:GetService("Players")
local RunService    = game:GetService("RunService")
local TweenService  = game:GetService("TweenService")
local UIS           = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService   = game:GetService("HttpService")
local LocalPlayer   = Players.LocalPlayer

-- =====================================================
--  WINDUI LOADER
-- =====================================================
local cloneref = cloneref or clonereference or function(i) return i end
local WindUI
do
    local ok, result = pcall(function()
        return require("./src/Init")
    end)
    if ok then
        WindUI = result
    else
        if cloneref(RunService):IsStudio() then
            WindUI = require(cloneref(ReplicatedStorage:WaitForChild("WindUI"):WaitForChild("Init")))
        else
            WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
        end
    end
end

-- =====================================================
--  CONFIG & STATE
-- =====================================================
local REQUIRE_TOOL_EQUIPPED = true
local JUMP_HEIGHT_THRESHOLD = 3.5
local JUMP_COOLDOWN         = 0.65
local MOVETO_REFRESH        = 0.12
local HOLD_DURATION         = 2.0
local DOUBLE_JUMP_POWER     = 0.6
local HITBOX_MAX_SIZE       = 4.4
local AUTOBOMB_COUNTDOWN    = 3
local AUTOBOMB_STUDS        = 15

local tracking      = false
local trackConn     = nil
local lastJump      = 0
local lastMoveTime  = 0
local currentTarget = nil

local autoBomb     = false
local autoBombConn = nil

local doubleJumpOn    = false
local doubleJumpUsed  = false
local doubleJumpConn  = nil
local doubleJumpInputConn = nil

local hitboxOn       = false
local hitboxSize     = 1.0

local enemyEspOn     = false
local enemyEspConn   = nil
local enemyEspHighlights = {}

local statusGuiOn    = false
local miniLocked     = false

-- Forward declared UI references for live updates
local uiRefs = {
    TargetVal = nil,
    StatusVal = nil,
    BombStatusVal = nil,
    TrackToggle = nil,
    BombToggle = nil,
    DJToggle = nil,
    HBToggle = nil,
    ESPToggle = nil,
    SGToggle = nil,
}

-- =====================================================
--  LOGIC HELPERS
-- =====================================================
local ESP_TAG = "__AutoTrackEnemyESP__"

local function hasGreenHighlight(char)
    if not char then return false end
    for _, v in ipairs(char:GetDescendants()) do
        if v:IsA("Highlight") and not v:GetAttribute(ESP_TAG) then
            local color = v.FillColor or v.OutlineColor
            if color and color.G > 0.5 and color.R < 0.4 and color.B < 0.4 then
                return true
            end
        end
    end
    return false
end

local function charHasTool(char)
    if not char then return false end
    for _, v in ipairs(char:GetChildren()) do
        if v:IsA("Tool") then return true end
    end
    return false
end

local function localHasTool()
    local char     = LocalPlayer.Character
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if char then
        for _, v in ipairs(char:GetChildren()) do
            if v:IsA("Tool") then return true end
        end
    end
    if REQUIRE_TOOL_EQUIPPED then return false end
    if backpack then
        for _, v in ipairs(backpack:GetChildren()) do
            if v:IsA("Tool") then return true end
        end
    end
    return false
end

local function getRoot()
    local c = LocalPlayer.Character
    return c and c:FindFirstChild("HumanoidRootPart")
end

local function getHum()
    local c = LocalPlayer.Character
    return c and c:FindFirstChildOfClass("Humanoid")
end

local function getArenaValue(player)
    local data = player:FindFirstChild("Data")
    if not data then return nil end
    local av = data:FindFirstChild("ActiveArena")
    if not av then return nil end
    local raw = av.Value
    if raw == nil then return nil end
    local s = tostring(raw)
    if s == "nil" or s == "" then return nil end
    return raw
end

local function getLocalArena()  return getArenaValue(LocalPlayer) end
local function getPlayerArena(p) return getArenaValue(p) end

local function findCountdownNumber(char)
    local bomb = char:FindFirstChild("Bomb")
    if not bomb then return nil, nil end
    local handle = bomb:FindFirstChild("BombHandle")
    if not handle then return nil, nil end
    local attachment = handle:FindFirstChild("UIAttachment")
    if not attachment then return nil, nil end
    local ui = attachment:FindFirstChild("UI")
    if not ui then return nil, nil end
    local label = ui:FindFirstChild("TimeLeft")
    if not label or not label:IsA("TextLabel") then return nil, nil end
    local txt = label.Text
    if not txt or txt == "" then return nil, nil end
    local n = tonumber(txt:match("(%d+%.?%d*)"))
    return n, txt
end

-- =====================================================
--  TRACKING LOOP
-- =====================================================
local function startTracking()
    if trackConn then return end
    lastJump = 0; lastMoveTime = 0; currentTarget = nil
    trackConn = RunService.Heartbeat:Connect(function()
        local now = tick()
        local char = LocalPlayer.Character
        local hasBomb = char and char:FindFirstChild("Bomb") ~= nil

        if autoBomb and not hasBomb then return end
        if not hasBomb then
            local h, r = getHum(), getRoot()
            if h and r then h:MoveTo(r.Position) end
            currentTarget = nil; return
        end

        local myArena = getLocalArena()
        if myArena == nil or tostring(myArena) == "nil" or tostring(myArena) == "" then
            local h, r = getHum(), getRoot()
            if h and r then h:MoveTo(r.Position) end
            currentTarget = nil; return
        end

        if now - lastMoveTime >= MOVETO_REFRESH then
            lastMoveTime = now
            local root = getRoot()
            local best, bestDist = nil, math.huge
            for _, p in ipairs(Players:GetPlayers()) do
                if p == LocalPlayer then continue end
                local pChar = p.Character
                if not pChar then continue end
                local pRoot = pChar:FindFirstChild("HumanoidRootPart")
                local pHum  = pChar:FindFirstChildOfClass("Humanoid")
                if not pRoot or not pHum or pHum.Health <= 0 then continue end
                if hasGreenHighlight(pChar) then continue end
                if charHasTool(pChar) then continue end
                local theirArena = getPlayerArena(p)
                if theirArena ~= myArena then continue end
                local d = (pRoot.Position - root.Position).Magnitude
                if d < bestDist then bestDist = d; best = p end
            end
            currentTarget = best
        end

        if not currentTarget then return end
        local root, hum = getRoot(), getHum()
        if not root or not hum or hum.Health <= 0 then return end
        local tChar = currentTarget.Character
        local tRoot = tChar and tChar:FindFirstChild("HumanoidRootPart")
        if not tRoot then currentTarget = nil; return end
        hum:MoveTo(tRoot.Position)
        local dy = tRoot.Position.Y - root.Position.Y
        if dy > JUMP_HEIGHT_THRESHOLD and now - lastJump >= JUMP_COOLDOWN then
            lastJump = now; hum.Jump = true
        end
    end)
end

local function stopTracking()
    if trackConn then trackConn:Disconnect(); trackConn = nil end
    currentTarget = nil
    local h, r = getHum(), getRoot()
    if h and r then h:MoveTo(r.Position) end
end

-- =====================================================
--  AUTO GET BOMB
-- =====================================================
local function startAutoBomb()
    if autoBombConn then return end
    autoBombConn = RunService.Heartbeat:Connect(function()
        local root, hum = getRoot(), getHum()
        if not root or not hum or hum.Health <= 0 then return end
        local myArena = getLocalArena()
        if not myArena then
            if uiRefs.BombStatusVal then uiRefs.BombStatusVal:SetTitle("NO ARENA") end
            hum:MoveTo(root.Position); return
        end
        if localHasTool() then
            if uiRefs.BombStatusVal then uiRefs.BombStatusVal:SetTitle("HOLDING BOMB") end
            return
        end
        local bestCarrier, bestChar, bestDist = nil, nil, math.huge
        for _, p in ipairs(Players:GetPlayers()) do
            if p == LocalPlayer then continue end
            local char = p.Character
            if not char then continue end
            local pRoot = char:FindFirstChild("HumanoidRootPart")
            local pHum  = char:FindFirstChildOfClass("Humanoid")
            if not pRoot or not pHum or pHum.Health <= 0 then continue end
            if getPlayerArena(p) ~= myArena then continue end
            local bombTool = char:FindFirstChild("Bomb")
            if not bombTool or not bombTool:IsA("Tool") then continue end
            local dist = (pRoot.Position - root.Position).Magnitude
            if dist > AUTOBOMB_STUDS then continue end
            if dist < bestDist then bestDist = dist; bestCarrier = p; bestChar = char end
        end
        if not bestCarrier then
            if uiRefs.BombStatusVal then uiRefs.BombStatusVal:SetTitle("NO BOMB") end
            hum:MoveTo(root.Position); return
        end
        local carrierRoot = bestChar:FindFirstChild("HumanoidRootPart")
        if not carrierRoot then return end
        local countNum, countTxt = findCountdownNumber(bestChar)
        local tooLate = countNum ~= nil and countNum <= 1
        local shouldGo = not tooLate and ((countNum == nil) or (countNum <= AUTOBOMB_COUNTDOWN))
        if shouldGo then
            hum:MoveTo(carrierRoot.Position)
            if uiRefs.BombStatusVal then
                local label = countTxt and ("GRABBING " .. countTxt) or "GRABBING"
                uiRefs.BombStatusVal:SetTitle(label .. "  [" .. math.floor(bestDist) .. "st]")
            end
        elseif tooLate then
            hum:MoveTo(root.Position)
            if uiRefs.BombStatusVal then uiRefs.BombStatusVal:SetTitle("TOO LATE  " .. (countTxt or "1")) end
        else
            hum:MoveTo(root.Position)
            if uiRefs.BombStatusVal then uiRefs.BombStatusVal:SetTitle("WAITING  " .. (countTxt or "")) end
        end
    end)
end

local function stopAutoBomb()
    if autoBombConn then autoBombConn:Disconnect(); autoBombConn = nil end
    local h, r = getHum(), getRoot()
    if h and r then h:MoveTo(r.Position) end
    if uiRefs.BombStatusVal then uiRefs.BombStatusVal:SetTitle("-") end
end

-- =====================================================
--  DOUBLE JUMP
-- =====================================================
local function stopDoubleJump()
    if doubleJumpConn then doubleJumpConn:Disconnect(); doubleJumpConn = nil end
    if doubleJumpInputConn then doubleJumpInputConn:Disconnect(); doubleJumpInputConn = nil end
    doubleJumpUsed = false
end

local function startDoubleJump()
    if doubleJumpConn then return end
    doubleJumpUsed = false
    local landedAt = tick()
    doubleJumpConn = RunService.Heartbeat:Connect(function()
        local hum = getHum()
        if not hum then return end
        local state = hum:GetState()
        if state == Enum.HumanoidStateType.Landed
            or state == Enum.HumanoidStateType.Running
            or state == Enum.HumanoidStateType.RunningNoPhysics
            or state == Enum.HumanoidStateType.Seated then
            doubleJumpUsed = false
            landedAt = tick()
        end
    end)
    doubleJumpInputConn = UIS.JumpRequest:Connect(function()
        if not doubleJumpOn then return end
        if tick() - landedAt < 0.2 then return end
        local hum = getHum()
        local root = getRoot()
        if not hum or not root then return end
        local state = hum:GetState()
        local airborne = state == Enum.HumanoidStateType.Freefall
            or state == Enum.HumanoidStateType.Jumping
        if airborne and not doubleJumpUsed then
            doubleJumpUsed = true
            local vel = root.AssemblyLinearVelocity
            root.AssemblyLinearVelocity = Vector3.new(vel.X, hum.JumpPower * DOUBLE_JUMP_POWER, vel.Z)
        end
    end)
end

-- =====================================================
--  HITBOX EXPANDER (Clone-based, original character untouched)
-- =====================================================
local hbClones = {} -- [char] = { [partName] = clonePart }
local hbConn = nil
local HB_WELD_NAME = "__HBWeld__"

local function getLimbParts(char)
    local parts = {}
    for _, child in ipairs(char:GetChildren()) do
        if child:IsA("BasePart") and (
            child.Name == "Head"
            or child.Name == "HumanoidRootPart"
            or child.Name:find("Torso")
            or child.Name:find("Arm")
            or child.Name:find("Leg")
        ) then
            parts[child.Name] = child
        end
    end
    return parts
end

local function startHitboxLoop()
    if hbConn then return end
    hbConn = RunService.RenderStepped:Connect(function()
        if not hitboxOn then return end
        for _, player in ipairs(Players:GetPlayers()) do
            if player == LocalPlayer then continue end
            local char = player.Character
            if not char then continue end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum or hum.Health <= 0 then continue end

            -- Create or update clones for this character
            if not hbClones[char] then hbClones[char] = {} end
            local limbParts = getLimbParts(char)

            for name, limb in pairs(limbParts) do
                local clone = hbClones[char][name]
                if not clone or not clone.Parent then
                    clone = Instance.new("Part")
                    clone.Name = "__HBClone_" .. name
                    clone.Transparency = 1
                    clone.CanCollide = false
                    clone.Massless = true
                    clone.Anchored = false
                    clone.CastShadow = false
                    clone.Material = Enum.Material.SmoothPlastic
                    clone.Color = Color3.fromRGB(255, 0, 0)
                    clone.Parent = char
                    hbClones[char][name] = clone
                end
                -- Match position/rotation of original limb every frame
                clone.CFrame = limb.CFrame
                clone.Size = limb.Size * hitboxSize
            end

            -- Clean up clones for limbs that no longer exist
            for cname, clone in pairs(hbClones[char]) do
                if not limbParts[cname] and clone and clone.Parent then
                    clone:Destroy()
                    hbClones[char][cname] = nil
                end
            end
        end
    end)
end

local function stopHitboxLoop()
    if hbConn then hbConn:Disconnect(); hbConn = nil end
    -- Destroy all clones
    for char, clones in pairs(hbClones) do
        for _, clone in pairs(clones) do
            if clone and clone.Parent then clone:Destroy() end
        end
    end
    hbClones = {}
end

-- =====================================================
--  ENEMY ESP
-- =====================================================
local function removeEspForPlayer(p)
    local hl = enemyEspHighlights[p]
    if hl and hl.Parent then hl:Destroy() end
    enemyEspHighlights[p] = nil
end

local function clearAllEnemyEsp()
    local keys = {}
    for p in pairs(enemyEspHighlights) do keys[#keys+1] = p end
    for _, p in ipairs(keys) do removeEspForPlayer(p) end
end

local function stopEnemyEsp()
    if enemyEspConn then enemyEspConn:Disconnect(); enemyEspConn = nil end
    clearAllEnemyEsp()
end

local function startEnemyEsp()
    if enemyEspConn then return end
    enemyEspConn = RunService.Heartbeat:Connect(function()
        local myArena = getLocalArena()
        for _, p in ipairs(Players:GetPlayers()) do
            if p == LocalPlayer then continue end
            local char = p.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not char or not hum or hum.Health <= 0
                or not myArena or getPlayerArena(p) ~= myArena then
                if enemyEspHighlights[p] then removeEspForPlayer(p) end
                continue
            end
            if hasGreenHighlight(char) then
                if enemyEspHighlights[p] then removeEspForPlayer(p) end
                continue
            end
            if not enemyEspHighlights[p] or not enemyEspHighlights[p].Parent then
                local hl = Instance.new("Highlight")
                hl:SetAttribute(ESP_TAG, true)
                hl.FillColor = Color3.fromRGB(255, 50, 50)
                hl.OutlineColor = Color3.fromRGB(200, 0, 0)
                hl.FillTransparency = 0.7
                hl.OutlineTransparency = 0
                hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                hl.Adornee = char
                hl.Parent = char
                enemyEspHighlights[p] = hl
            end
        end
        for p in pairs(enemyEspHighlights) do
            if not Players:FindFirstChild(p.Name) then removeEspForPlayer(p) end
        end
    end)
end

-- =====================================================
--  STATUS GUI OVERLAY
-- =====================================================
local StatusWin, SW_TrackVal, SW_BombVal, SW_FpsVal, SW_PingVal
do
    local SG = Instance.new("ScreenGui")
    SG.Name = "AutoTrackStatusOverlay"
    SG.ResetOnSpawn = false
    SG.DisplayOrder = 999
    SG.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    SG.Parent = LocalPlayer:WaitForChild("PlayerGui")

    StatusWin = Instance.new("Frame", SG)
    StatusWin.Name = "StatusGui"
    StatusWin.Size = UDim2.new(0, 180, 0, 112)
    StatusWin.Position = UDim2.new(1, -208, 0, 60)
    StatusWin.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
    StatusWin.BorderSizePixel = 0
    StatusWin.Active = true
    StatusWin.Draggable = true
    StatusWin.Visible = false
    StatusWin.ZIndex = 80
    Instance.new("UICorner", StatusWin).CornerRadius = UDim.new(0, 12)
    local swStroke = Instance.new("UIStroke", StatusWin)
    swStroke.Color = Color3.fromRGB(50, 50, 50)
    swStroke.Thickness = 1
    swStroke.Transparency = 0.3
    swStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    local accent = Instance.new("Frame", StatusWin)
    accent.Size = UDim2.new(0.4, 0, 0, 2)
    accent.Position = UDim2.new(0.3, 0, 0, 0)
    accent.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    accent.BorderSizePixel = 0
    accent.ZIndex = 82
    Instance.new("UICorner", accent).CornerRadius = UDim.new(0, 2)

    local hdr = Instance.new("Frame", StatusWin)
    hdr.Size = UDim2.new(1, 0, 0, 28)
    hdr.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
    hdr.BorderSizePixel = 0
    hdr.ZIndex = 81
    Instance.new("UICorner", hdr).CornerRadius = UDim.new(0, 12)

    local hdrFill = Instance.new("Frame", StatusWin)
    hdrFill.Size = UDim2.new(1, 0, 0, 14)
    hdrFill.Position = UDim2.new(0, 0, 0, 14)
    hdrFill.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
    hdrFill.BorderSizePixel = 0
    hdrFill.ZIndex = 81

    local title = Instance.new("TextLabel", StatusWin)
    title.Size = UDim2.new(1, -12, 0, 28)
    title.Position = UDim2.new(0, 10, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "STATUS"
    title.TextColor3 = Color3.fromRGB(170, 170, 170)
    title.TextSize = 9
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.ZIndex = 84

    local div = Instance.new("Frame", StatusWin)
    div.Size = UDim2.new(1, -16, 0, 1)
    div.Position = UDim2.new(0, 8, 0, 28)
    div.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    div.BorderSizePixel = 0
    div.ZIndex = 82

    local function makeSWRow(labelTxt, yOff)
        local k = Instance.new("TextLabel", StatusWin)
        k.Size = UDim2.new(0, 58, 0, 14)
        k.Position = UDim2.new(0, 10, 0, yOff)
        k.BackgroundTransparency = 1
        k.Text = labelTxt
        k.TextColor3 = Color3.fromRGB(65, 65, 65)
        k.TextSize = 9
        k.Font = Enum.Font.GothamBold
        k.TextXAlignment = Enum.TextXAlignment.Left
        k.ZIndex = 83
        local v = Instance.new("TextLabel", StatusWin)
        v.Size = UDim2.new(1, -72, 0, 14)
        v.Position = UDim2.new(0, 68, 0, yOff)
        v.BackgroundTransparency = 1
        v.Text = "-"
        v.TextColor3 = Color3.fromRGB(180, 180, 180)
        v.TextSize = 9
        v.Font = Enum.Font.GothamBold
        v.TextXAlignment = Enum.TextXAlignment.Left
        v.ZIndex = 83
        return v
    end

    SW_TrackVal = makeSWRow("TRACK", 34)
    SW_BombVal  = makeSWRow("BOMB", 50)
    SW_FpsVal   = makeSWRow("FPS", 68)
    SW_PingVal  = makeSWRow("PING", 84)
end

local fpsCounter = 0
local fpsLast = tick()
local fpsDisplay = 0

local function updateStatusGui()
    if not statusGuiOn then return end
    if tracking then
        if not localHasTool() then
            SW_TrackVal.Text = "NO BOMB"
            SW_TrackVal.TextColor3 = Color3.fromRGB(200, 130, 60)
        elseif currentTarget then
            SW_TrackVal.Text = currentTarget.Name
            SW_TrackVal.TextColor3 = Color3.fromRGB(160, 210, 160)
        else
            SW_TrackVal.Text = "SCANNING"
            SW_TrackVal.TextColor3 = Color3.fromRGB(130, 160, 210)
        end
    else
        SW_TrackVal.Text = "OFF"
        SW_TrackVal.TextColor3 = Color3.fromRGB(80, 80, 80)
    end
    if autoBomb then
        SW_BombVal.Text = uiRefs.BombStatusVal and uiRefs.BombStatusVal.Title or "ON"
        SW_BombVal.TextColor3 = Color3.fromRGB(180, 180, 180)
    else
        SW_BombVal.Text = "OFF"
        SW_BombVal.TextColor3 = Color3.fromRGB(80, 80, 80)
    end
    fpsCounter = fpsCounter + 1
    local now = tick()
    if now - fpsLast >= 1 then
        fpsDisplay = fpsCounter
        fpsCounter = 0
        fpsLast = now
    end
    SW_FpsVal.Text = tostring(fpsDisplay)
    SW_FpsVal.TextColor3 = fpsDisplay >= 55 and Color3.fromRGB(140, 210, 140)
        or fpsDisplay >= 30 and Color3.fromRGB(210, 180, 80)
        or Color3.fromRGB(210, 80, 80)
    local ok, ping = pcall(function()
        return math.floor(LocalPlayer:GetNetworkPing() * 1000)
    end)
    if ok and ping then
        SW_PingVal.Text = tostring(ping) .. " ms"
        SW_PingVal.TextColor3 = ping < 80 and Color3.fromRGB(140, 210, 140)
            or ping < 150 and Color3.fromRGB(210, 180, 80)
            or Color3.fromRGB(210, 80, 80)
    else
        SW_PingVal.Text = "-"
        SW_PingVal.TextColor3 = Color3.fromRGB(80, 80, 80)
    end
end

-- =====================================================
--  LIVE UPDATE LOOP
-- =====================================================
RunService.Heartbeat:Connect(function()
    if tracking then
        if not localHasTool() then
            if uiRefs.StatusVal then uiRefs.StatusVal:SetTitle("NO TOOL") end
            if uiRefs.TargetVal then uiRefs.TargetVal:SetTitle("-") end
        elseif currentTarget then
            if uiRefs.StatusVal then uiRefs.StatusVal:SetTitle("TRACKING") end
            if uiRefs.TargetVal then uiRefs.TargetVal:SetTitle(currentTarget.Name) end
        else
            if uiRefs.StatusVal then uiRefs.StatusVal:SetTitle("SCANNING") end
            if uiRefs.TargetVal then uiRefs.TargetVal:SetTitle("-") end
        end
    else
        if uiRefs.StatusVal then uiRefs.StatusVal:SetTitle("IDLE") end
        if uiRefs.TargetVal then uiRefs.TargetVal:SetTitle("-") end
    end
    updateStatusGui()
end)

-- =====================================================
--  WINDUI INTERFACE
-- =====================================================
local Window = WindUI:CreateWindow({
    Title = "Timebomb Duels",
    Folder = "autotrack",
    Icon = "solar:radar-bold",
    Theme = "Crimson",
    NewElements = true,
    HideSearchBar = false,
    OpenButton = {
        Title = "Timebomb Duels | V1",
        CornerRadius = UDim.new(1, 0),
        StrokeThickness = 2,
        Enabled = true,
        Draggable = true,
        OnlyMobile = false,
        Scale = 0.6,
        Color = ColorSequence.new(
            Color3.fromHex("#FF4830"),
            Color3.fromHex("#E0E0E0")
        ),
    },
    Topbar = {
        Height = 44,
        ButtonsType = "Default",
    },
})

Window:Tag({
    Title = "v1",
    Icon = "tag",
    Color = Color3.fromHex("#FF4830"),
    Border = true,
})

-- ==================== MAIN TAB ====================
local MainTab = Window:Tab({
    Title = "Main",
    Icon = "solar:home-2-bold",
    IconColor = Color3.fromHex("#83889E"),
    IconShape = "Square",
    Border = true,
})

-- Live Status
local StatusSection = MainTab:Section({ Title = "Live Status" })

uiRefs.TargetVal = StatusSection:Paragraph({
    Title = "Target",
    Desc = "-",
})
uiRefs.StatusVal = StatusSection:Paragraph({
    Title = "Status",
    Desc = "IDLE",
})

-- Auto Track
MainTab:Space()
uiRefs.TrackToggle = MainTab:Toggle({
    Title = "Auto Track",
    Desc = "Auto tracks the nearest player",
    Value = false,
    Callback = function(state)
        tracking = state
        if tracking then startTracking() else stopTracking() end
    end,
})

-- Auto Get Bomb
MainTab:Space()
local BombSection = MainTab:Section({
    Title = "Auto Get Bomb",
    Box = true,
    BoxBorder = true,
    Opened = true,
})

uiRefs.BombToggle = BombSection:Toggle({
    Title = "Enable Auto Get Bomb",
    Desc = "Automatically grab bomb from nearest carrier",
    Value = false,
    Callback = function(state)
        autoBomb = state
        if autoBomb then startAutoBomb() else stopAutoBomb() end
    end,
})

BombSection:Slider({
    Title = "Grab Countdown Threshold",
    Desc = "Grab when carrier countdown <= this value",
    Step = 1,
    Value = { Min = 1, Max = 10, Default = AUTOBOMB_COUNTDOWN },
    Callback = function(v) AUTOBOMB_COUNTDOWN = v end,
})

BombSection:Slider({
    Title = "Max Studs to Carrier",
    Desc = "Only attempt grab within this range",
    Step = 1,
    Value = { Min = 5, Max = 100, Default = AUTOBOMB_STUDS },
    Callback = function(v) AUTOBOMB_STUDS = v end,
})

uiRefs.BombStatusVal = BombSection:Paragraph({
    Title = "Bomb Status",
    Desc = "-",
})

-- Hitbox Expander
MainTab:Space()
local HBSection = MainTab:Section({
    Title = "Hitbox Expander",
    Box = true,
    BoxBorder = true,
    Opened = true,
})

uiRefs.HBToggle = HBSection:Toggle({
    Title = "Enable Hitbox Expander",
    Desc = "Uniform scale on all body parts (no launch)",
    Value = false,
    Callback = function(state)
        hitboxOn = state
        if hitboxOn then startHitboxLoop() else stopHitboxLoop() end
    end,
})

HBSection:Slider({
    Title = "Hitbox Size Multiplier",
    Desc = "Max 4.4x — applied every frame",
    IsTooltip = true,
    Step = 0.1,
    Value = { Min = 1.0, Max = HITBOX_MAX_SIZE, Default = 1.0 },
    Callback = function(v)
        hitboxSize = math.clamp(math.floor(v * 10 + 0.5) / 10, 1.0, HITBOX_MAX_SIZE)
    end,
})

-- Movement (moved from Settings)
MainTab:Space()
local MoveSection = MainTab:Section({
    Title = "Movement",
    Box = true,
    BoxBorder = true,
    Opened = true,
})

uiRefs.DJToggle = MoveSection:Toggle({
    Title = "Double Jump",
    Desc = "One extra jump while airborne",
    Value = false,
    Callback = function(state)
        doubleJumpOn = state
        if doubleJumpOn then startDoubleJump() else stopDoubleJump() end
    end,
})

MoveSection:Slider({
    Title = "Double Jump Power",
    Desc = "Multiplier of normal JumpPower",
    IsTooltip = true,
    Step = 0.05,
    Value = { Min = 0.3, Max = 1.0, Default = DOUBLE_JUMP_POWER },
    Callback = function(v)
        DOUBLE_JUMP_POWER = math.clamp(math.floor(v * 100 + 0.5) / 100, 0.3, 1.0)
    end,
})

-- Visuals (moved from Settings)
MainTab:Space()
local VisSection = MainTab:Section({
    Title = "Visuals",
    Box = true,
    BoxBorder = true,
    Opened = true,
})

uiRefs.ESPToggle = VisSection:Toggle({
    Title = "Enemy ESP",
    Desc = "Red highlight on enemies in your arena (skips green teammates)",
    Value = false,
    Callback = function(state)
        enemyEspOn = state
        if enemyEspOn then startEnemyEsp() else stopEnemyEsp() end
    end,
})

uiRefs.SGToggle = VisSection:Toggle({
    Title = "Status Overlay",
    Desc = "Draggable FPS/Ping/Track status panel",
    Value = false,
    Callback = function(state)
        statusGuiOn = state
        StatusWin.Visible = state
    end,
})

-- Keybind (moved from Settings)
MainTab:Space()
MainTab:Keybind({
    Title = "Toggle Auto Track Keybind",
    Desc = "Press to toggle tracking on/off",
    Value = "F",
    Callback = function(keyName) end,
})

-- ==================== SETTINGS TAB (WindUI Config Only) ====================
local SettingsTab = Window:Tab({
    Title = "Settings",
    Icon = "solar:settings-bold",
    IconColor = Color3.fromHex("#7775F2"),
    IconShape = "Square",
    Border = true,
})

-- UI Appearance
local UISection = SettingsTab:Section({
    Title = "UI Appearance",
    Box = true,
    BoxBorder = true,
    Opened = true,
})

UISection:Toggle({
    Title = "Transparent Panel Background",
    Desc = "Remove window background for cleaner look",
    Value = false,
    Callback = function(state)
        Window:SetPanelBackground(not state)
    end,
})

UISection:Slider({
    Title = "UI Scale",
    Desc = "Adjust overall UI size",
    IsTooltip = true,
    Step = 0.05,
    Value = { Min = 0.6, Max = 1.2, Default = 1.0 },
    Callback = function(v)
        Window:SetUIScale(v)
    end,
})

-- Config Manager
SettingsTab:Space()
local ConfigSection = SettingsTab:Section({
    Title = "Config Manager",
    Box = true,
    BoxBorder = true,
    Opened = true,
})

local ConfigManager = Window.ConfigManager
local configName = "default"

local configNameInput = ConfigSection:Input({
    Title = "Config Name",
    Placeholder = "Enter config name...",
    Value = "default",
    Callback = function(value)
        configName = value
    end,
})

ConfigSection:Space()

local allConfigs = ConfigManager:AllConfigs()
local defaultVal = table.find(allConfigs, configName) and configName or nil

local configsDropdown = ConfigSection:Dropdown({
    Title = "Saved Configs",
    Desc = "Select an existing config",
    Values = allConfigs,
    Value = defaultVal,
    Callback = function(value)
        configName = value
        configNameInput:Set(value)
    end,
})

ConfigSection:Space()

ConfigSection:Button({
    Title = "Save Config",
    Justify = "Center",
    Callback = function()
        Window.CurrentConfig = ConfigManager:Config(configName)
        if Window.CurrentConfig:Save() then
            WindUI:Notify({
                Title = "Config Saved",
                Content = "Config '" .. configName .. "' saved successfully",
                Icon = "check",
            })
        end
        configsDropdown:Refresh(ConfigManager:AllConfigs())
    end,
})

ConfigSection:Space()

ConfigSection:Button({
    Title = "Load Config",
    Justify = "Center",
    Callback = function()
        Window.CurrentConfig = ConfigManager:CreateConfig(configName)
        if Window.CurrentConfig:Load() then
            WindUI:Notify({
                Title = "Config Loaded",
                Content = "Config '" .. configName .. "' loaded successfully",
                Icon = "refresh-cw",
            })
        end
    end,
})

-- ==================== ABOUT TAB ====================
local AboutTab = Window:Tab({
    Title = "About",
    Icon = "solar:info-square-bold",
    IconColor = Color3.fromHex("#10C550"),
    IconShape = "Square",
    Border = true,
})

AboutTab:Section({
    Title = "Timebomb Duels",
    TextSize = 22,
    FontWeight = Enum.FontWeight.Bold,
})

AboutTab:Space()

AboutTab:Section({
    Title = "Features:\n• Auto Track — nearest target in same arena\n• Auto Get Bomb — countdown-aware smart grabbing\n• Double Jump — configurable airborne boost\n• Hitbox Expander — uniform scale, no-launch engine\n• Enemy ESP — red outline, skips green teammates\n• Status Overlay — draggable FPS/Ping/Track panel\n• Config Manager — save/load presets\n\nUI powered by WindUI by Footagesus.",
    TextSize = 14,
    TextTransparency = 0.3,
    FontWeight = Enum.FontWeight.Medium,
})

AboutTab:Space({ Columns = 2 })

AboutTab:Button({
    Title = "Destroy Window",
    Color = Color3.fromHex("#ff4830"),
    Justify = "Center",
    Icon = "shredder",
    IconAlign = "Left",
    Callback = function()
        stopTracking()
        stopAutoBomb()
        stopDoubleJump()
        stopHitboxLoop()
        stopEnemyEsp()
        StatusWin.Parent:Destroy()
        Window:Destroy()
    end,
})

-- =====================================================
--  INIT
-- =====================================================
updateStatusGui()